# Copy this file to .env (gitignored) and fill in your own value.
# Your Apple Developer Team ID, used for code signing when generating the
# Xcode project and deploying to devices (scripts/deploy.sh loads it).
# Find it under Xcode > Settings > Accounts, or developer.apple.com > Membership.
LEDGE_DEVELOPMENT_TEAM=YOURTEAMID
